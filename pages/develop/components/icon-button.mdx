import { MdxLayout } from "@/components/layouts/mdx";
import { LiveArea } from "@/components/live-area";
import { Props } from "@/components/props";
import { IconButton } from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Icon button"
    description="When the action a button invoke is so clear that needs no words, just an icon"
    showMeta
    features={[
      "Keyboard accessible",
      "Focusable even when disabled",
      "Multiple styles and sizes",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design pattern",
        url: "https://www.w3.org/TR/wai-aria-1.2/#button",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#button",
      },
    ]}
    hero={
      <LiveArea direction="row">
        <IconButton icon="bell" />
      </LiveArea>
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```jsx
import { IconButton } from "@wonderflow/react-components";

export default () => {
  return <IconButton icon="plus">Click</IconButton>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "kind",
      type: ['"primary"', '"secondary"', '"flat"'],
      default: '"primary"',
      required: false,
      dangerous: false,
      description: "Set the element visual style",
    },
    {
      name: "dimension",
      type: ['"small"', '"regular"', '"big"'],
      default: '"regular"',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
    {
      name: "icon",
      type: ["IconNames"],
      default: "",
      required: true,
      dangerous: false,
      description: "Set the icon to show inside the element",
    },
    {
      name: "disabled",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the disabled state",
    },
    {
      name: "onClick",
      type: ["(MouseEvent<HTMLButtonElement | HTMLAnchorElement>) => void"],
      default: "",
      required: false,
      dangerous: false,
      description: "Callback for the click event",
    },
    {
      name: "as",
      type: ["keyof JSX.IntrinsicElements | React.ComponentType<any>"],
      default: '"button"',
      required: false,
      dangerous: false,
      description:
        "Set the component to a different HTML tag or custom component.",
    },
  ]}
/>

## Accessibility

Adheres to the [`button` role requirements](https://www.w3.org/TR/wai-aria-1.2/#button). This component always requires the `aria-label` or `aria-labelledby` attributes to describe the action.

#### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
  ]}
/>
