import { MdxLayout } from "@/components/layouts/mdx";
import { LiveArea } from "@/components/live-area";
import { Props } from "@/components/props";
import { Text } from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Text"
    description="Dynamic and fluid typography with control"
    showMeta
    showPlayground
    features={[
      "Dynamic HTML tag",
      "Fully customizzable",
      "Responsive text",
      "Themes ready",
    ]}
    hero={
      <LiveArea>
        <Text>
          Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
          dolores, tempore quas labore officiis praesentium. Porro sed dolorem,
          numquam temporibus.
        </Text>
      </LiveArea>
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```jsx highlight=1,4
import { Text } from "@wonderflow/react-components";

export default () => {
  return <Text>Click</Text>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "size",
      type: ["14", "16", "18", "22", "28"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the text font-size",
    },
    {
      name: "dimmed",
      type: ["4", "5", "6", "7"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the text color to match dimmed color themes",
    },
    {
      name: "sentiment",
      type: ["positive", "informative", "danger", "warning"],
      default: "",
      required: false,
      dangerous: false,
      description:
        "Set the text color. This changes based on the current theme",
    },
    {
      name: "weight",
      type: ["thin", "bold"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the weight of the text",
    },
    {
      name: "fluid",
      type: ["boolean"],
      default: "true",
      required: false,
      dangerous: false,
      description: "Set the responsive font-size",
    },
    {
      name: "as",
      type: ["keyof JSX.IntrinsicElements | React.ComponentType<any>"],
      default: '"p"',
      required: false,
      dangerous: false,
      description:
        "Set the component to a different HTML tag or custom component.",
    },
  ]}
/>
