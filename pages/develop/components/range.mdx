import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { Props } from "@/components/props";
import * as Components from "@wonderflow/react-components";

export const example = `<Range aria-label="Items count" showValues />`;

export default ({ children }) => (
  <MdxLayout
    title="Range"
    description="An input where the user selects a value from within a given range."
    showMeta
    features={[
      "Multiple sizes",
      "Can be controlled or uncontrolled",
      "Supports touch or click on track to update value",
      "Supports Right to Left direction",
      "Full keyboard navigation",
    ]}
    hero={<Playground scope={{ ...Components }} code={example} />}
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Range } from "@wonderflow/react-components";

export default () => {
  return <Range />;
};
```

## Examples

### With icons

If you don't want to use numeric values you can use icons instead.

export const exampleWithIcons = `<Range aria-label="brightness" iconMin="moon" iconMax="sun-bright" />`;

<Playground scope={{ ...Components }} code={exampleWithIcons} />

## API Reference

<Props
  properties={[
    {
      name: "min",
      type: ["number"],
      default: "0",
      required: false,
      dangerous: false,
      description: "The minimum value",
    },
    {
      name: "max",
      type: ["number"],
      default: "100",
      required: false,
      dangerous: false,
      description: "The maximum value",
    },
    {
      name: "step",
      type: ["number"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the incrementation step",
    },
    {
      name: "onInput",
      type: ["(value: number) => void"],
      default: "",
      required: false,
      dangerous: false,
      description:
        "Callback for the input event. The current value is passed as argument",
    },
    {
      name: "defaultValue",
      type: ["number"],
      default: "0",
      required: false,
      dangerous: false,
      description: "The initial value",
    },
    {
      name: "showValues",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Show the current value and max. This hides icons.",
    },
    {
      name: "iconMin",
      type: ["IconNames"],
      default: "",
      required: false,
      dangerous: false,
      description: "Use icon for the min value",
    },
    {
      name: "iconMax",
      type: ["IconNames"],
      default: "",
      required: false,
      dangerous: false,
      description: "Use icon for the max value",
    },
    {
      name: "dimension",
      type: ['"small"', '"regular"'],
      default: '"regular"',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
  ]}
/>
