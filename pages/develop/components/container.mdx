import { MdxLayout } from "@/components/layouts/mdx";
import { LiveArea } from "@/components/live-area";
import { Props } from "@/components/props";
import { Container } from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Container"
    description="Who contains the content"
    showMeta
    showPlayground
    features={[
      "Auto centering",
      "Predefined sizes",
      "Auto adjust paddings",
      "Fluid by default",
    ]}
    hero={
      <LiveArea>
        <Container dimension="large" className="ContainerEx"></Container>
      </LiveArea>
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```jsx highlight=1,4
import { Container } from "@wonderflow/react-components";

export default () => {
  return <Container>...</Container>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "dimension",
      type: ['"full"', '"medium"', '"large"'],
      default: '"full"',
      required: false,
      dangerous: false,
      description: "Set the element max width",
    },
    {
      name: "padding",
      type: ["boolean"],
      default: "true",
      required: false,
      dangerous: false,
      description: "Enable or disable the predefined padding",
    },
    {
      name: "as",
      type: ["keyof JSX.IntrinsicElements | React.ComponentType<any>"],
      default: '"div"',
      required: false,
      dangerous: false,
      description:
        "Set the component to a different HTML tag or custom component.",
    },
  ]}
/>
