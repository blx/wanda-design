import { MdxLayout } from "@/components/layouts/mdx";
import { Playground } from "@/components/playground";
import { Props } from "@/components/props";
import { Checklist } from "@/components/checklist";
import * as components from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Button"
    description="A button is a box area or text that communicates and triggers user actions when clicked."
    showMeta
    features={[
      "Keyboard accessible",
      "Focusable even when disabled",
      "Can contain icons",
      "Multiple styles and sizes",
      "Themes ready",
    ]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#button",
      },
      {
        label: "ARIA authoring",
        url: "https://www.w3.org/TR/wai-aria-practices-1.2/#button",
      },
    ]}
    hero={
      <Playground
        liveAreaProps={{ direction: "row" }}
        scope={{ ...components }}
        code={`<Button icon="check">Button</Button>`}
      />
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```tsx highlight=1,4
import { Button } from "@wonderflow/react-components";

export default () => {
  return <Button>Click</Button>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "kind",
      type: ['"primary"', '"secondary"', '"flat"'],
      default: '"primary"',
      required: false,
      dangerous: false,
      description: "Set the element visual style",
    },
    {
      name: "dimension",
      type: ['"small"', '"regular"', '"big"'],
      default: '"regular"',
      required: false,
      dangerous: false,
      description: "Set the element size",
    },
    {
      name: "fullWidth",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the element to fill the available space",
    },
    {
      name: "icon",
      type: ["IconNames"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the icon to show inside the element",
    },
    {
      name: "iconPosition",
      type: ['"left"', '"right"'],
      default: '"left"',
      required: false,
      dangerous: false,
      description: "Set the icon position",
    },
    {
      name: "disabled",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the disabled state",
    },
    {
      name: "loading",
      type: ["boolean"],
      default: "",
      required: false,
      dangerous: false,
      description: "Set the loading state and show the spinner",
    },
    {
      name: "onClick",
      type: ["(MouseEvent<HTMLButtonElement | HTMLAnchorElement>) => void"],
      default: "",
      required: false,
      dangerous: false,
      description: "Callback for the click event",
    },
    {
      name: "as",
      type: ["keyof JSX.IntrinsicElements | React.ComponentType<any>"],
      default: '"button"',
      required: false,
      dangerous: false,
      description:
        "Set the component to a different HTML tag or custom component",
    },
  ]}
/>

## Accessibility

Adheres to the [`button` role requirements](https://www.w3.org/TR/wai-aria-1.2/#button).

### Keyboard interactions

<Props
  properties={[
    {
      name: "tab",
      description: "Move focus on the element or outside",
    },
    {
      name: "enter",
      description: "Trigger the component action",
    },
  ]}
/>
