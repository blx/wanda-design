import { MdxLayout } from "@/components/layouts/mdx";
import { LiveArea } from "@/components/live-area";
import { Props } from "@/components/props";
import { Separator } from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Separator"
    description="Visual separation of content"
    showMeta
    showPlayground
    features={["Fill the available space by default", "Themes ready"]}
    links={[
      {
        label: "ARIA design",
        url: "https://www.w3.org/TR/wai-aria-1.2/#separator",
      },
    ]}
    hero={
      <LiveArea fill>
        <Separator />
      </LiveArea>
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```jsx
import { Separator } from "@wonderflow/react-components";

export default () => {
  return <Separator />;
};
```

## Accessibility

Adheres to the [`separator` role requirements](https://www.w3.org/TR/wai-aria-1.2/#separator).

The separator renders a **hr** element. If you are just adding visual
separation and not a content-related separation you must add **aria-hidden** attribute.
