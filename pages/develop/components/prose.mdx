import { MdxLayout } from "@/components/layouts/mdx";
import { LiveArea } from "@/components/live-area";
import { Props } from "@/components/props";
import { Prose, Text, Title } from "@wonderflow/react-components";

export default ({ children }) => (
  <MdxLayout
    title="Prose"
    description="Vertical rhythm for text content"
    showMeta
    showPlayground
    features={[
      "Auto space titles and text content",
      "Useful for pages with many text elements",
    ]}
    hero={
      <LiveArea maxHeight="50vh">
        <Prose>
          <Title as="h1" level="4">
            Title
          </Title>
          <Text>
            Lorem ipsum dolor sit, amet consectetur adipisicing elit. Eius
            dolores, tempore quas labore officiis praesentium.
          </Text>
        </Prose>
      </LiveArea>
    }
  >
    {children}
  </MdxLayout>
);

## Anatomy

```jsx
import { Prose } from "@wonderflow/react-components";

export default () => {
  return <Prose>...</Prose>;
};
```

## API Reference

<Props
  properties={[
    {
      name: "as",
      type: ["keyof JSX.IntrinsicElements | React.ComponentType<any>"],
      default: '"div"',
      required: false,
      dangerous: false,
      description:
        "Set the component to a different HTML tag or custom component.",
    },
  ]}
/>
